import { OffloadModifyType } from './offload-modify-type';

describe('OffloadModifyType', () => {
  it('should create an instance', () => {
    expect(new OffloadModifyType()).toBeTruthy();
  });
});
