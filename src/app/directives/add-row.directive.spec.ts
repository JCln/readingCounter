import { AddRowDirective } from './add-row.directive';

describe('AddRowDirective', () => {
  it('should create an instance', () => {
    const directive = new AddRowDirective();
    expect(directive).toBeTruthy();
  });
});
