import { UnSortTableDirective } from './un-sort-table.directive';

describe('UnSortTableDirective', () => {
  it('should create an instance', () => {
    const directive = new UnSortTableDirective();
    expect(directive).toBeTruthy();
  });
});
